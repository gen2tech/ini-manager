# ini-manager
The simple php code will read and write ini files

It can parse a given INI file and parses it to extract the configuration values into an associative array using its section names as keys to store the configuration values contained in each section.

It can also list all the sections in the parsed INI file.

The class can also create a new INI file from an associative array with the section names and configuration values in each section.
